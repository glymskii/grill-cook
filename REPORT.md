# Определение длительности жарки котлеты на каждой стороне по видео — исследование осуществимости

**Дата:** 2026-07-11 · **Статус:** прототип работает на реальном видео

## TL;DR

Задача технологически осуществима и уже решена коммерчески (Miso Robotics CookRight, PreciTaste) — камеры над грилем + CV-детекция каждой котлеты, алерты «переверни/снимай». Наш прототип на open-vocabulary детекторе (YOLO-World) + ByteTrack + цветовой детектор переворота подтверждает это на реальном YouTube-видео со статичной камерой: укладка, перевороты (включая повторные) и снятие фиксируются, каждой котлете присваивается track_id, таймеры привязаны к стороне, касающейся плиты.

## Результаты на тестовом видео

Сверка с ручной покадровой разметкой (12 достоверных событий, 3 котлеты):

| Событие | Ground truth | Пайплайн | Ошибка |
|---|---|---|---|
| Укладка T | 6.6s | 6.6s | 0.0s |
| Укладка BL | 7.2s | 7.1s | 0.1s |
| Укладка BR | 7.9s | 7.5s | 0.4s |
| Переворот №1 T | 33.0s | 34.5s | 1.5s |
| Переворот №1 BL | 34.4s | 34.8s | 0.4s |
| Переворот №1 BR | 36.0s | 34.7s | 1.3s |
| Переворот №2 T | 58.5s | 59.3s | 0.8s |
| Переворот №2 BL | 60.5s | 59.2s | 1.3s |
| Переворот №2 BR | 59.5s | **пропущен** | — |
| Снятие T | 68.0s | 69.0s | 1.0s |
| Снятие BL | 69.0s | 71.1s | 2.1s |
| Снятие BR | 70.0s | 69.4s | 0.6s |

**Итог: recall 11/12 (92%), precision 100% (0 ложных событий), медианная ошибка времени <1s.** Каждая котлета провела весь цикл (укладка→снятие) под одним track_id, включая переживание окклюзий лопаткой, стеклянной крышки и монтажных склеек. Третий (неполный) раунд переворотов на 64–66s не был засчитан ни пайплайном, ни уверенно человеком (смазанные кадры) — исключён из метрики. Пропуск переворота №2 BR — серия быстрых переворотов трёх котлет подряд + крышка сразу после.

Пример выходной сводки (`summary.csv`): котлета №1 — сторона 1: 37.7s, сторона 2: 24.7s, 2 переворота; таймеры корректно продолжались после повторного переворота.

<!-- RESULTS -->

## Архитектура прототипа

```
Видео (статичная камера над плитой)
  │
  ├─ 1. Детекция котлет ─ YOLO-World L (open-vocabulary, промпты
  │      "food"/"meat"/"fried meat patty"/"round slice of raw minced meat"),
  │      class-agnostic NMS, фильтры размера/аспекта бокса, ROI сковороды
  │
  ├─ 2. Трекинг ─ ByteTrack (пониженные пороги под low-conf детекции)
  │      + «сшивка» треков: после потери ≤3s новый трек в радиусе 1.3 диаметра
  │      котлеты наследует историю (переворот лопаткой рвёт сырые треки)
  │
  ├─ 3. Детектор склеек ─ diff фоновой рамки кадра вне ROI сковороды;
  │      на склейке сбрасываются цветовые буферы и глушится детекция
  │      переворота (защита от ложных FLIP на монтаже/зуме)
  │
  └─ 4. State machine на каждый track_id:
        PLACED  → таймер стороны 1 (сторона, коснувшаяся плиты)
        FLIP    → фикс текущего таймера, запуск/продолжение таймера другой
                  стороны (произвольное число переворотов, ретро-коррекция
                  момента переворота)
        REMOVED → стоп обоих таймеров
        Переворот = ступенчатое изменение цвета (медиана Lab центрального
        эллипса кропа, ΔE recent-vs-baseline) + буст чувствительности при
        скачке/окклюзии бокса (действие лопаткой)
```

Выход: аннотированное видео (track_id + оба таймера на каждой котлете), `events.json` (все события с таймкодами), `summary.csv` (сводка по котлетам).

## Тестовые данные

Поиск вёлся мульти-агентным веб-сёрчем (28 кандидатов). Основное видео: **"How to Cook Frozen Beef Hamburger Patties In A Pan"** (YouTube `03MrKdBr-Gc`, 95s) — фиксированная камера строго над сковородой, 3 котлеты, полный цикл: укладка → переворот №1 (поштучно) → переворот №2 → частично №3 → снятие. Сырая сторона розовая, жареная коричневая — сильный цветовой сигнал переворота. Осложнения, приближающие к реальности: монтажные склейки (12 минут готовки сжаты в 95s), зум-кадры, стеклянная крышка (62–63s), окклюзии рукой/лопаткой.

Ground truth размечен вручную по кадрам (`data/frozen_patties_gt.json`).

Резервные видео для стресс-тестов: «Как жарить котлеты» (6:46, 6 котлет, крышка, ракурс сбоку), «Куриные котлеты» (6 котлет, чистая съёмка), стоковые клипы Pexels.

## Prior art (подтверждение осуществимости)

| Система/работа | Что делает | Вывод для нас |
|---|---|---|
| **Miso Robotics CookRight** | Камеры над грилем + CV: распознаёт тип/толщину мяса, ведёт таймер каждого объекта, алертит «пора переворачивать/снимать» | Наша задача в проде уже решена; валидирует подход |
| **Miso патент US 12,462,547** | Клеймит цикл: камеры → модель определяет identity+location каждого объекта на гриле → расчёт времени до flip/remove | Юридически заявленная схема = наша архитектура |
| **PreciTaste / RealSense** | Потолочные RGB-D камеры над грилями QSR (~$2000 + $399/мес) | Проверенная сенсорика: RGB-D сверху с ~2 м |
| **VLM cooking-state (arXiv 2403.08239)** | CLIP-подобие кадр↔текст как непрерывная кривая состояния готовки | Альтернативный детектор переворота/готовности без обучения |
| **Steak doneness CNN (DenseNet121, 95.3%)** | Классификация прожарки по кадру, edge-развёртывание | Пер-кроп классификатор «сырая/жареная сторона» достижим малыми данными |
| **Roboflow burger-datasets (~4.7k изображений)** | Готовые размеченные датасеты патти | Быстрый путь к дообученному детектору вместо zero-shot |
| **50 Salads / MPII Cooking** | Статичные overhead-камеры, экшн-разметка готовки | Бенчмарки с нашей геометрией камеры |

Открытого готового open-source решения «flip-tracking на гриле» не найдено — ниша свободна, собирать придётся из компонентов (что мы и сделали).

## Что оказалось сложным (уроки прототипа)

1. **Zero-shot детекция котлет нестабильна.** YOLO-World с промптом "burger patty" теряет сырые розовые котлеты на зум-кадрах (0 детекций при conf 0.03). Помог комбинированный сет промптов ("food", "meat", "fried meat patty", "round slice of raw minced meat") + class-agnostic NMS + фильтры размера/аспекта. Conf жареных котлет 0.2–0.75, сырых 0.1–0.3 — для прода нужен дообученный детектор (данные есть: Roboflow ~4.7k патти).
2. **Трекер рвёт трек в момент переворота** (окклюзия лопаткой + быстрое перемещение). Решение: слой «сшивки» — новый трек в радиусе 1.3 диаметра в течение 3s наследует историю и таймеры. Радиус больше расстояния между котлетами недопустим — «краденые» ID соседок.
3. **Детекция переворота по цвету работает**, ΔE(Lab) шага «сырая↔жареная» = 8–13.5 при пороге 11 (8 при окклюзионном бусте). Второй переворот (жареная↔жареная) — сигнал слабее, здесь один пропуск из трёх.
4. **Монтажные склейки — главный источник ложных переворотов.** Детектор склеек по 4 сторонам фоновой рамки (склейка меняет ≥3 стороны, рука — одну) убрал все ложные FLIP. На реальной статичной камере проблемы нет вовсе — это артефакт YouTube-монтажа.
5. **Крышка/пар/руки** — короткие потери детекции (≤3s) сшиваются без потери таймера.

## Промышленная плита заказчика (`data/Котлета Жарка.mp4`)

Реальная съёмка с производственной кухни: 6:05, 1080×1920, 50 fps, жёстко закреплённая камера, одна котлета, полный цикл включая добавление сыра. Прогон с прореживанием до 10 fps (для таймингов с точностью ~0.1 с этого достаточно, скорость обработки ×5).

> **Правка от 2026-08-06 по замечанию заказчика.** Первоначальные цифры ниже оказались неверны: на 2:47 повар выполняет не один переворот, а серию из ~4 манипуляций (переворот → котлету ставят на ребро и катают → переворот обратно), в результате которой котлета **возвращается на первоначальную сторону A**. А на 3:36 происходит настоящий последний переворот, который алгоритм принял за добавление сыра. Выверенная покадрово разметка — в `data/kotleta_zharka_gt.json`. Верные величины:
>
> | | Значение |
> |---|---|
> | Сторона A (первая на плите) | **203.1 с (3:23)** |
> | Сторона B | **150.9 с (2:31)** |
> | Вне плиты (в воздухе/на ребре) | 1.3 с |
> | Итого на плите | **355.3 с (5:55)** |
> | Чистых переворотов | 3 (167.5 / 172.7 / 216.6 с) |
> | Эпизодов манипуляций | 5 |
>
> Ошибка первой версии: 44 секунды жарки были записаны не той стороне (сторона A занижена на 22%, сторона B завышена на 31%). Разбор причин и текущий статус исправления — в разделе «Ограничение: серии быстрых переворотов» ниже.

**Замеры первой версии алгоритма (с ошибкой в раскладке по сторонам):**

| Величина | Алгоритм | Ручная разметка | Ошибка |
|---|---|---|---|
| Укладка на плиту | 8.3 с | 8.3 с | 0.0 с |
| Переворот | 166.7 с | 167.2 с (отрыв лопаткой) | 0.5 с |
| Добавление сыра | 216.2 с, помечено `TOPPING` | 219 с | 2.8 с |
| Снятие с плиты | — (видео обрывается) | 363.6 с | — |
| **Сторона 1** | **158.4 с (2:38)** | 158.9 с | 0.5 с (0.3%) |
| **Сторона 2** | **197.9 с (3:18)** | 191.6 с | 6.3 с (3.3%) |
| **Итого на плите** | **356.3 с (5:56)** | 355.3 с | 1.0 с (0.3%) |

Расхождение по стороне 2 объяснимо и управляемо: котлета находится в воздухе на лопатке ~4.8 с (167.2→172.0), и текущая модель относит это время ко второй стороне. Для продакшена это вопрос соглашения — либо исключать «время в воздухе» из обоих таймеров, либо оставить как есть.

**Что пришлось исправить по итогам этого видео** (все правки проверены регрессией на эталонном датасете — 11/12 событий сохранились):

1. **Разрыв трека на перевороте.** Когда повар поднимает котлету на лопатке, её бокс уезжает вверх по кадру на 1.5 диаметра (котлета приближается к камере). Радиус сшивки 1.3 диаметра не дотягивался → одна котлета распадалась на два track_id. Увеличен до 2.2.
2. **Двойное срабатывание переворота.** Манёвр длится 4–5 с, всё это время внешний вид меняется; кулдаун 3 с успевал истечь и событие фиксировалось дважды. Кулдаун поднят до 12 с + после переворота сбрасывается буфер цветовых признаков.
3. **Сыр распознавался как переворот** (ΔE=40 и 104). Введён класс события `TOPPING`: обе стороны котлеты — мясо, поэтому переворот меняет цвет ограниченно (по обоим датасетам ΔE 8–20), а скачок выше 30 означает, что на котлету что-то положили. Таймер активной стороны при этом продолжает идти — физически верно.
4. **Таймаут снятия** поднят с 3.5 до 9 с, чтобы подъём котлеты в воздух не читался как снятие.

## Ограничение: серии быстрых переворотов

Замечание заказчика вскрыло принципиальный, а не параметрический дефект.

**Почему первая версия ошиблась.** Она искала *ступеньку цвета* в скользящем окне: сравнивала медиану Lab за 2 секунды «до» с медианой за 0.6 секунды «после». Отдельные манипуляции в серии на 2:47 длятся 0.4–1.1 секунды — то есть **быстрее, чем само окно сравнения**, поэтому вся серия схлопывалась в одно событие. Кулдаун 12 секунд, введённый чтобы один переворот не считался дважды, довершал дело. Отдельно: переворот на 3:36 и сыр на 3:38 попали в одно окно, и сыр (ΔE=83) перекрыл переворот (ΔE=12).

**Что сделано.** Написан `src/manipulations.py` — детекция, привязанная к физическому событию вместо скользящего окна. Логика: сторона, лежащая на плите, может смениться только когда котлета оторвана от поверхности, поэтому сначала находятся все *эпизоды манипуляции* (котлета в воздухе, перекрыта рукой или стоит на ребре — по обвалу яркости, скачку бокса вверх и росту вытянутости), а затем сравнивается вид котлеты в устоявшихся кадрах непосредственно до и после каждого эпизода.

Результат: **все 5 эпизодов манипуляций находятся с точностью 0.2–0.5 с**, включая те, что первая версия не видела вовсе. Переворот на 3:36 теперь классифицируется как переворот, а не как сыр.

**Что не работает.** Решение «перевернули или только подвинули» по-прежнему принимается по средней Lab-разнице, а к середине жарки обе стороны котлеты становятся почти одного цвета: подрумяненная сторона A даёт a≈137, посеревшая сторона B — a≈141 при близкой яркости. На эпизоде катания по ребру разница между «до» и «после» составила ΔE=12.2 — выше порога переворота (10), хотя лицо котлеты не изменилось. Алгоритм засчитал лишний переворот, чётность сбилась, и итоговая раскладка на полном видео получилась 306.6 / 44.7 с вместо верных 203.1 / 150.9.

Средний цвет как признак был исчерпан, поэтому подход заменён целиком — см. следующий раздел.

## Решение: классификатор лица котлеты (финальная версия)

Вместо поиска *изменений* пайплайн теперь покадрово определяет, **какая сторона котлеты обращена к камере**. Сторона, лежащая на плите, — противоположная видимой. Скорость переворотов при этом перестаёт иметь значение: нет окна сравнения, которое можно «перебежать».

**Датасет** (`src/build_face_dataset.py`, 530 кропов из этого же видео по выверенной разметке):

| Класс | Кропов | Откуда |
|---|---|---|
| `FACE_B` — сторона B вверх | 229 | 10–167 с и 173–216 с |
| `FACE_A` — сторона A вверх | 103 | три окна общей длительностью 2.3 с |
| `HANDLING` — в воздухе / на ребре | 101 | пять эпизодов манипуляций |
| `TOPPING` — под сыром | 97 | 219–363 с |

**Модель:** ImageNet-ResNet18 как замороженный экстрактор признаков + логистическая регрессия. При сотне примеров редкого класса дообучать саму сеть бессмысленно — она их просто запомнит.

**Проверка честная, сплит по эпизодам, не случайный.** Кропы, снятые с интервалом 40 мс, — почти дубликаты; при случайном разбиении копии одного мгновения попали бы и в train, и в test. В отложенную выборку вынесены ровно те эпизоды, где ошибалась прошлая версия: переворот на 3:36 (`A_3`) и весь стретч 173–216 с (`B_late`).

Первый прогон дал 70% и вскрыл, чем модель «жульничает»: она выучила «сторона B = розовая сырая», а к 173-й секунде эта сторона посерела — 26 кадров из 72 ушли в FACE_A. Тон оказался ложной подсказкой, которая перестаёт работать к середине жарки. Лечится аугментацией: сильный джиттер яркости/контраста/насыщенности плюс случайное обесцвечивание убирают цвет как признак и оставляют текстуру корки.

Результат на отложенных эпизодах — **точность 83.8%**, и главное:

```
        предсказано:  A    B    H    T
истина A          [ 33    0    0    0 ]   переворот 3:36 — 33/33
истина B          [  0   50   22    0 ]
истина HANDLING   [  1    0   14    4 ]
истина TOPPING    [  0    0    0   43 ]
```

**Путаницы между сторонами A и B нет ни одной** (было 26). Оставшиеся ошибки — B→HANDLING: они не меняют сторону, а лишь ставят таймер на паузу, и гасятся сглаживанием по окну 0.32 с.

**Итог на полном видео** (`src/analyze_faces.py`, 25 fps):

| Величина | Алгоритм | Ручная разметка | Ошибка |
|---|---|---|---|
| Переворот 1 | 167.6 с | 167.5 с | 0.1 с |
| Переворот 2 (возврат на A) | 172.8 с | 172.7 с | 0.1 с |
| Переворот 3 | 217.0 с | 216.6 с | 0.4 с |
| Сыр | 218.8 с | 218.4 с | 0.4 с |
| **Сторона A** | **202.5 с (3:22)** | 203.1 с | 0.6 с (0.3%) |
| **Сторона B** | **148.0 с (2:28)** | 150.9 с | 2.9 с (1.9%) |
| Вне плиты | 6.4 с | — | катание по ребру отнесено сюда |
| **Итого на плите** | **356.8 с (5:57)** | 355.3 с | 1.5 с (0.4%) |

Ровно три переворота в верном порядке, обе стороны сходятся с ручной разметкой. Расхождение по стороне B — это ~2 секунды катания по ребру, которые разметка отнесла к стороне B, а алгоритм считает временем вне плиты; спорный случай, и трактовка алгоритма скорее корректнее.

### Версия 3: два домена и отказ от класса «в руках»

Классификатор v1 не переносился на другую кухню — на сгенерированных клипах с другой плитой и другим фаршем он метил 40% кадров как `HANDLING`, и таймеры оставались пустыми. Дообучение на втором домене (крупы из клипов Kling, 504 штуки) само по себе не помогло — точность на отложенных эпизодах даже упала до 57%.

Причина оказалась не в данных, а в составе классов. `HANDLING` («котлета в воздухе, на ребре или перекрыта рукой») — семантически размытая категория, и после добавления второго домена она стала самой малочисленной (101 против 426 у `FACE_B`). При `class_weight="balanced"` это дало ей огромный вес, и она начала засасывать всё неоднозначное — в первую очередь посеревшую сторону B.

Решение: **убрать `HANDLING` из классификатора совсем**. Факт «котлету держат» — свойство трека, а не картинки: детектор и так теряет или резко смещает бокс, когда котлету поднимают. В пайплайне эту роль теперь играет порог уверенности: если ни один класс не набирает 0.55, кроп считается нечитаемым.

Результат на отложенных эпизодах — **100% по всем трём классам**, включая `B_late` (посеревшая сторона B, где v1 давал 39%, а v2 после цветовой аугментации — 69%):

```
        предсказано:  A    B    T
истина A          [ 33    0    0 ]
истина B          [  0   72    0 ]
истина TOPPING    [  0    0   43 ]
```

На полностью невиданном клипе (генерация Kling, другая плита, другой фарш, другой свет) трекинг даёт 15 стабильных ID из 15 котлет, стороны определяются верно, найдено 7 переворотов вместо 2 у прежней версии.

### Сквозная проверка v3

**На реальном видео заказчика** дообучение не сломало исходную точность, а слегка улучшило её:

| | v1 | v3 | Эталон |
|---|---|---|---|
| Сторона A | 202.5 с | **204.6 с** | 203.1 с |
| Сторона B | 148.0 с | **150.4 с** | 150.9 с |
| Итого на плите | 356.8 с | 356.8 с | 355.3 с |

Ошибка по обеим сторонам — в пределах 0.3–0.7%. Переворот на 3:36 и сыр на 3:38 фиксируются верно.

Ухудшение одно: на эпизоде катания по ребру (2:47–2:52) v3 выдаёт 4 события переворота вместо двух. Убрав класс `HANDLING`, мы лишились естественного сигнала «этот кадр читать не надо», и классификатор пытается определить сторону даже у котлеты, стоящей на ребре. Чётность при этом сошлась, поэтому итоговые таймеры верны, но события шумят. Чинится порогом уверенности выше 0.55 либо связкой с `manipulations.py`: не засчитывать смену стороны, пока трек показывает разрыв или скачок бокса.

**На композите** проверка вскрыла настоящий баг трекинга: ByteTrack переиспользует свои идентификаторы, а код доверял этому соответствию безусловно — из-за чего история одной котлеты «телепортировалась» на другую. Ассоциация переписана на жадное сопоставление по расстоянию (котлеты на плите неподвижны, поэтому позиция — самый надёжный признак). Медианная ошибка момента укладки упала с 4.6 до 2.4 с.

Остальные расхождения на композите оказались дефектом самого композита, а не пайплайна: спрайт сыра, вырезанный из реального видео, при вставке даёт бледное пятно, которое детектор не опознаёт как еду вовсе — пять котлет из пятнадцати пропадают из кадра сразу после добавления сыра и больше не находятся. Плюс часть слотов заехала на решётку у края плиты. **В текущем виде композит невалиден как эталон**, и приводить по нему цифры точности некорректно. Для проверки мульти-объектного режима лучше годятся сгенерированные клипы Kling — там реальная сцена без артефактов монтажа.

**Оговорка о применимости.** Классификатор v1 был обучен на кропах из одного ролика, поэтому его показатели именно на нём были завышены. Честная оценка обобщения — цифры на отложенных эпизодах выше. Для другой кухни, другого фарша или другого ракурса потребуется дособрать кропы и переобучить: это дёшево (полчаса на прогон разметки + минута на обучение), но обязательно.

**Что это значит для запуска.** Раскладка по сторонам теперь верна и для быстрых серий переворотов с подпеканием боков — то есть для реальной работы повара, а не только для учебного цикла «положил–перевернул–снял».

## Стресс-тест: профессиональная кухня (Five Guys)

Вторая волна поиска (5 агентов) дала ~20 кандидатов из настоящих бургерных: Five Guys (сетевой гриль, 10–20 котлет), White Manna NJ (25–30 слайдеров с луком), Redhot Ranch Chicago, корейские бургерные (Yummyboy, Food Kingdom — штативные съёмки). Прогнан сегмент Five Guys 1:40–2:55 (`data/fiveguys.mp4`, партия ~12 котлет: укладка → переворот → сыр → снятие).

**Что подтвердилось:**
- Детекция масштабируется: 10–12 котлет на промышленном гриле трекаются одновременно с индивидуальными ID даже zero-shot моделью, включая сырые розовые и дожаренные.
- Скорость профессиональной готовки (смэш-котлеты, 2–4 мин цикл) сама по себе проблем не создаёт.

**Что сломалось (и почему это ценный результат):**
- Съёмка велась «плавающей» камерой оператора (Travel Thirsty), а не фикс-камерой: каждая перекадровка дробит треки — 23 логических ID на ~15 физических котлет, PLACED/REMOVED часто означают «камера сменила план», а не физическое событие. Наша архитектура (фиксированный ROI, cut-детектор, сшивка по координатам) принципиально рассчитана на статичную камеру.
- Промпты "food"/"meat" ловят булочки и топпинги как «котлеты» — нужен дообученный класс-специфичный детектор.
- Перевороты почти не пойманы: моменты флипов совпадают с перекадровками (подавление), плюс у смэш-котлет меньше цветовой контраст сторон.

**Вывод стресс-теста:** публичные видео из профессиональных кухонь снимаются операторами и для валидации таймеров непригодны — но отлично годятся как источник обучающих данных для детектора (тысячи кадров котлет на промышленных грилях). Для продакшена в бургерной обязательна собственная жёстко смонтированная камера над грилем (ровно так делают CookRight/PreciTaste) — тогда работают все механизмы, проверенные на основном тесте.

## Ограничения прототипа и путь к продакшену

| Область | Прототип | Продакшен |
|---|---|---|
| Детектор | Zero-shot YOLO-World L (~7 fps на M-серии CPU/MPS) | YOLOv8/11-s, дообученный на 2–5k кадров своей плиты → 30+ fps, conf >0.8 |
| Сторона котлеты | Смена стороны = ступень цвета ΔE | + классификатор кропа «сырая/жареная сторона» (DenseNet-класс задач, 95%+ достижимо) — устойчив к жареная↔жареная |
| Переворот | Цвет + скачок бокса | + детекция руки/лопатки (hand keypoints) как триггер-подтверждение |
| Камера | Чужое YouTube-видео, склейки | Своя фикс-камера над плитой (как CookRight/PreciTaste), опционально +depth/термо |
| Валидация | 1 видео, 3 котлеты, ручная разметка | Датасет из своих записей, автометрики per-event |

Ключевой вывод: **все обнаруженные проблемы — артефакты чужого монтированного видео, в контролируемой постановке (своя статичная камера, без склеек) задача проще, чем тестовая.** Коммерческие аналоги (CookRight) подтверждают продакшен-жизнеспособность.

## Файлы проекта

- `src/run_pipeline.py` — CLI: `python src/run_pipeline.py video.mp4 --roi x1,y1,x2,y2 --out out/run`
- `src/detector.py` / `src/patties.py` / `src/config.py` — детекция, трекинг+сшивка+state machine, конфиг
- `src/probe.py` — проба промптов детекции на кадрах, `src/compare_gt.py` — сверка с разметкой
- `out/final/annotated.mp4` — аннотированное видео (track_id, стороны, живые таймеры)
- `out/final/events.json`, `summary.csv` — события и сводка
- `data/frozen_patties_gt.json` — ручная разметка ground truth
- `data/fiveguys.mp4`, `data/whitemanna.mp4` — видео из профессиональных бургерных (Five Guys; White Manna NJ — 25–30 слайдеров, наисложнейший кейс: лук и булочки поверх котлет)
- `out/fiveguys/annotated_720p.mp4` — стресс-тест на Five Guys

## Real-time RTSP бенчмарк на пилотном сервере (M5 Max), 25.08.2026

Топология теста = топологии пилота: ffmpeg-паблишер → mediamtx (:8555, TCP-only)
→ OpenCV/FFMPEG клиент → детектор → ByteTrack. Источник: реальный клип кухни,
1080×1920@25, 5 Мбит (эмуляция IP-камеры). Скрипт: `src/bench_rtsp.py`.

| прогон | обработано fps | детекция p50/p95, мс | e2e p50/p95, мс | пропуски |
|---|---|---|---|---|
| дообученная @960, **5 мин** | **24.3 из 24.4** | 13.3 / 17.2 | 14.6 / 18.7 | 0.0 |
| дообученная @640, 90 с | 22.6 из 22.9 | 13.1 / 16.8 | 14.4 / 18.2 | 0.01 |

Выводы:
- Сервер обрабатывает **каждый кадр** потока 25 fps с запасом ~×3 (13 мс на кадр
  при бюджете 40 мс). Термодеградации за 5 минут нет: по минутам 21.4 (разгон),
  25.0, 25.0, 24.98, 25.0.
- 640 не быстрее 960 — на M5 Max доминируют накладные MPS, а не свёртки.
  Остаёмся на 960 (лучше ловит мелкие/дальние котлеты).
- Запас позволяет добавить классификатор стороны и/или 2–3 камеры на один сервер.
- Грабли по пути (важно для продовых инсталляций): занятые порты — qemu сидел и
  на 8554, и на 8000/UDP; лечится TCP-only relay на выделенном порту + catch-all
  `paths` в конфиге mediamtx; паблишер обязан слать `-rtsp_transport tcp`.

## Live-детектор смены сцены (25.08.2026)

Порт оффлайн-схемы в edge-конвейер: 96×54 gray, 4 бордер-полосы, статистика =
3-я по величине полоса. Порог сделан адаптивным — `max(9.0, EMA×5)`, EMA
обновляется только вне катов. Калибровка: реальная камера p99=0.9/max=2.1
(порог 9 — нулевые ложные), склейка Kling p50=2.3 (генеративный шум ×20 к
живой камере). На кат — мягкий сброс: alive-котлеты уходят событием
`invalidated` (не `removed` — не попадают в аналитику как завершённые),
1.2 с settle-окно, HUD показывает «View changed — tracking reset».

Прогоны склейки (2 цикла): без детектора — призраки до +1:52 и 18/15 на плите;
фиксированный порог 9 — 15 катов, треки не доживают (0 flips); адаптивный —
8 катов по швам, 7 честных flips, призраков нет.

Попутно закрыта дыра устойчивости: worker кэширует run-state и восстанавливает
его при рестарте хаба — жарка не остановится из-за редеплоя облака.

## Детектор v2 на реальных мульти-котлеточных видео (28.08.2026)

Партнёр прислал два реальных видео (640×368, пережаты мессенджером):
IMG_4138 — говядина+курица обычной жарки, IMG_4139 — смэш. Куриные ловились
и v1 (conf 0.6–0.9) — дорабатывать не пришлось. Слепые пятна v1: тёмная
прожаренная корка на тёмной плите и сыр поверх котлеты; их не видел и
zero-shot разметчик.

Решение — темпоральная пропагация слотов: котлета неподвижна, поэтому
детекции обеих моделей кластеризуются в стационарные слоты, и кадры внутри
жизни слота размечаются даже там, где обе модели слепы (767 кадров / 5515
боксов, сплит по времени 70/30, негативы с пустой плитой). Дообучение с v1,
30 эпох. Combined-val mAP50 0.73 → 0.79.

| видео | метрика | v1 | v2 |
|---|---|---|---|
| 4138 | котлет/кадр · пустых · conf | 7.4 · 1/109 · 0.72 | **9.6 · 0 · 0.85** |
| 4139 смэш | котлет/кадр · пустых · conf | 1.9 · 15/108 · 0.52 | **4.2 · 0 · 0.87** |

Сырные смэши: 2/4 → 4/4 при conf 0.92+. Тёмный ряд: размечается почти
полностью. Побочный эффект: v2 видит готовые котлеты и на лотке выдачи —
нужен ROI плиты в конвейере (бэклог). Синтетика убрана из вэла; в трейне
остаётся до накопления 3–4 реальных кухонь.

## Full HD и линейка v2–v6 (28.08.2026, вечер)

Партнёр прислал оригиналы: IMG_6635 (полная сессия 16:46, 1080p60 — пережатка
была вырезкой 8:53) и IMG_6637 (смэш). Найдены и закрыты две системные слепоты
самообучения: (1) гейт max_aspect 1.8 выбрасывал ракурсно сплющенные дальние
котлеты из всех кругов разметки — модели учили их как фон (гейт → 2.6 +
crop-zoom учитель по дальней зоне); (2) допуск разрыва слота 45 с склеивал
«сняли→положили» в фантомы на пустой плите (→ 18 с), а остаточный фантом
пятна добила Full HD-разметка.

Единый вэл (синтетика + Full HD): v2 0.625 → v6 0.786 mAP50 (0.556 → 0.698
mAP50-95). Зона розовых 0/21 → 21/21, фантомы 0, Full HD 15.2 котлет/кадр без
пустых. Пилот переведён на v6.

## Сравнение архитектур: YOLOv8s (v6) vs RF-DETR-Small (28.08.2026, ночь)

Обе на одном Full HD датасете (12.5k боксов). Полевые пробы: паритет
(розовые 21/21 vs 20/21, фантомы центра 0/0, Full HD 15.2 vs 14.7 кот/кадр);
RF-DETR чист на пенном фантоме кромки (v6 — стойкий 0.76) и даёт mAP50 0.834
против 0.786 (разные харнессы); латентность MPS 27 мс против 17 мс — оба
реалтайм. Вывод: архитектура не была узким местом (весь рост v2→v6 дали
данные), но RF-DETR (Apache-2.0) назначается миграционной целью до первого
платящего клиента — снимает AGPL-вопрос при равном качестве. Пилот остаётся
на v6. Скрипт: src/train_rfdetr.py, конвертер: src/yolo_to_coco.py.

## Замер времён по Full HD и находка про идентичность треков (30.08.2026)

Линейный прогон всей 16-минутной сессии через боевую логику (src/measure_cook_times.py,
10 fps, без зацикливания) вместо зацикленного стрима, который рвал треки.

Первый результат обнажил дефект: **291 «переворот» на 128 котлет** — конвейер
считал переворотом любое перекрытие рукой или лопаткой. Ужесточение (пауза
≥1.2 с, ≥25 с на стороне, кулдаун 30 с) убрало ложные, но вскрыло главное:

| число засчитанных переворотов | котлет | медиана на плите |
|---|---|---|
| 0 | 92 | **4 с** |
| 1 | 16 | 54 с |
| 2 | 11 | 89 с |
| 3+ | 9 | 247–861 с |

92 трека из 128 живут секунды — это не котлеты, а обрывки: при плотной укладке
жадная ассоциация по ближайшему центру перекидывает детекции между соседями,
а перекрытия лопаткой добивают. Чистых случаев (1 переворот, обе стороны >20 с)
— **2 из 128**. Норматив прожарки по такому видео не выводится; цели выставлены
прагматично (A=120, B=90, ±30) до получения спеки кухни.

**Приоритет №1 перед пилотом — устойчивость идентичности трека**: ассоциация по
IoU/венгерскому алгоритму вместо ближайшего центра, переживание длинных
перекрытий, сшивка треков после возврата. Всё остальное (детектор, зоны,
таймеры, аналитика) уже сильнее этого звена.

Также внедрено: размерный гейт (медиана радиуса ±0.58–1.7, отсекает
негабаритные ложные боксы), затемнение вне рабочей зоны в HUD, точки-якоря
в центре котлет.

## Устойчивость идентичности трека (30.08.2026)

Переписана ассоциация: венгерский алгоритм вместо жадного ближайшего центра,
стоимость = дистанция + штраф размера + Lab-цвет центра котлеты; узкий гейт
(1.45r, расширяется только для пропавшего трека); кладбище с воскрешением
(60 с, после 12 с — при совпадении цвета, чтобы сырая котлета на месте снятой
не унаследовала чужой таймер); физика твёрдых тел — наслоившиеся треки
сливаются (дубль умирает, история старшего сохраняется); дедуп совпавших
боксов до трекера.

Метрика-судья — «телепорты»: скачок видимого трека >0.8r за кадр = кража чужой
детекции (класс ошибки, который приписывает время не той котлете и невидим
счётчикам id). A/B на полном FullHD (зона плиты включена):

| | id | обрывки | медиана жизни | ТЕЛЕПОРТЫ |
|---|---|---|---|---|
| было (жадная, 3.2r) | 84 | 30 | 16с | **163** |
| стало (1.45r) | 98 | 22 | 18с | **0** |

Ошибки идентичности устранены полностью; остаточная фрагментация (22 обрывка,
47 слияний) — материал следующей итерации (удержание unmatched-треков,
Калман-сглаживание). Ложные детекции «кожи» оказались бортиком плиты и лотком
(35 штук вне зоны за 9 кадров) — режутся зоной, размерным гейтом и дедупом.

## Добивание фрагментации: ядро в прод, КФ — в исследовательскую ветку (31.08.2026)

Пять A/B-итераций на полном FullHD с метриками телепортов и видимых треков.
Ключевой урок №1: KF-скорость на джиттере боксов (±0.1r) случайно блуждает до
1.6 r/s и рушит трекер (210 телепортов) — шумные юнит-тесты теперь обязательны.
Урок №2: «медиана треков в кадре» без разделения видимые/призраки вводит в
заблуждение — у всех движков видимых ровно 13, различия были в призраках.

В прод — «ядро»: венгерская ассоциация + физика твёрдых тел (дедуп совпавших
боксов, слияние призрак-на-живом с неприкосновенностью видимых пар):

| | id | обрывки | жизнь | видимых | слияния | телепорты |
|---|---|---|---|---|---|---|
| жадная | 84 | 30 | 16с | 13 | — | 163 |
| венгр (вчера) | 98 | 22 | 18с | 13 | 47 | 0 |
| **ядро (прод)** | **77** | 21 | 18с | 13 | **26** | **0** |
| исслед. +КФ | 88 | 16 | 25с | 13 | 49 | 9 |

КФ-ветка (жизнь трека 25с, обрывки 16) осталась за флагом use_kf=False: платит
9 телепортами — включим после настройки на размеченной MOT-истине.

## MOT-истина и настройка КФ по IDF1 (31.08.2026)

Инфраструктура: src/dump_dets.py (детекции окна одним GPU-проходом) →
src/build_mot_gt.py (истина с полным хиндсайтом: жёсткие треклеты, сшивка
через паузы с проверкой занятости места, ревью-кадры для ручной верификации,
вето-файлы data/mot_overrides_*.json) → src/tune_kf_idf1.py (решётка параметров,
IDF1/MOTA/switches через py-motmetrics, tune + holdout).

Истина: tune 27 треков (3 мин), holdout 30 треков (2 мин). Ручная проверка 16
длинных склеек нашла одну ложную (полоса времени показала пустое место посреди
28-с паузы — котлету сняли), вето применено.

Настройка: base IDF1 0.784 (tune) / 0.860 (holdout). Лучший КФ (β=0.3,
DEAD=0.009, VMAX=0.1): 0.821 / 0.860, switches 4→2 — формальный победитель.
Но финальная телепорт-проверка на полных 16 минутах: 6 телепортов (ядро 0),
слияний 70 (26), медиана жизни 7с (18с). Размеченное окно покрывает плотную
готовку; телепорты живут в переходах (старт, уборка) — там КФ по-прежнему
вреден. По правилу «корректность старше плавности» ядро остаётся в проде,
КФ — за флагом use_kf с настроенными константами.

Вывод: гибрид (КФ только для треков старше N секунд в устойчивой фазе) —
кандидат следующей итерации; мерить обеими осями (IDF1 на истине + телепорты
на полной сессии) — теперь это полчаса, инфраструктура постоянная.

## Переходные окна и вердикт по гибриду (31.08.2026)

Доразмечены окна вокруг двух упрямых телепортов КФ (t=489 перекладка лопаткой,
t=945 смэш-прессовка): 18+15 GT-треков, два переноса котлет досклеены вручную
(force_merges) после сверки содержимого — иначе GT наказывал бы трекер за
правильное удержание id через перенос.

Матрица (полный дамп 5 fps, 4 GT-окна): ядро побеждает 3:1 — trans1 IDF1
0.981 против 0.950 у всех КФ-вариантов (перенос лучше обрабатывается
graveyard-воскрешением, чем инерцией фильтра), trans2 паритет, tune за КФ
(+0.037), holdout равенство. Полная сессия: ядро 0 телепортов / 9 слияний /
жизнь 107с против 2 / 40+ / 52–61с у КФ и гибридов. Возрастной затвор гибрида
не помогает: телепорты происходят у зрелых треков.

Решение: ядро остаётся в проде, гибрид отклонён по доказательствам обеих осей.
Фрагментация стартовой точки (92/128 обрывков, жизнь 4с) закрыта: медиана
жизни 107с, слияний 9. Инфраструктура (полный дамп детекций, 4 GT-окна с
вето/force-файлами, CPU-матрица src/eval_matrix.py) — постоянная: цикл
«гипотеза → цифры» занимает минуту.

## Покадровый аудит смэш-демо: два дефекта, найденные заказчиком (31.08.2026)

**Фантомные котлеты.** Детектор был не при чём: на t=88 ровно 6 детекций на 6
котлет (conf 0.71–0.91), но в трекере жило 10 треков — мёртвые ждали
removed_after до удаления и рисовались наравне с живыми. Дисплей (HUD и
рендерер) теперь показывает только треки, видимые в последние 0.4 с; история
для воскрешения остаётся внутри движка. Лишних колец: 4 разом → 0.23 на кадр
в среднем. removed_after 9с → 6с.

**Перевороты мимо.** Контактная лента: повар переворачивает лопаткой на
55–75 с; движок рапортовал 104/115/152/165. Причина фундаментальная — детектор
переворота ждал ИСЧЕЗНОВЕНИЯ котлеты, а смэш-переворот происходит на месте,
из кадра патти не пропадает. Добавлен второй сигнал — корка: при перевороте
L* падает на 35–50 за ~2.5с (сырое→зажаренное), при укладке сыра растёт и
уходит в жёлтый (b* +25). Направление разделяет события. Результат: перевороты
на 60/62/62/67с для четырёх треков (стороны A 32–43с), сыр на 95–106с не
считается переворотом. Флаги: colour_win/flip_dl/topping_db.

Вывод процесса: покадровый аудит демо-ролика обязателен перед показом —
агрегированные метрики (телепорты, IDF1) этих двух дефектов не видели.

## Покадровый аудит первого видео: экспозиция и лопатка (31.08.2026)

Аудит 16.8-минутной сессии вскрыл два ложных источника «переворотов», которых
не было в смэш-видео:

1. **Автоэкспозиция.** Когда повар наклоняется, камера темнит всю плиту, и
   сигнал корки срабатывал у всех котлет разом — залпы по 14–16 штук
   (t=352, 403, 436, 470). Проверено данными: моменты залпов совпадают с
   глобальными провалами медианной яркости. Первая попытка вычитать глобальную
   яркость сделала хуже (130→232: при осветлении сцены коррекция усиливала
   сигнал). Правильно — сравнивать котлету с **медианой изменения соседей** за
   то же окно и с тем же сглаживанием.
2. **Снятие лопаткой.** На t=712–718 повар снимает котлеты; тень и перекрытие
   лопаткой темнят их на 1–3 с. Настоящий переворот оставляет корку тёмной
   навсегда — добавлено подтверждение стойкости (crust_confirm=4 с), кредит
   времени отдаётся моменту самого переворота, а не подтверждения.

Итог: первое видео 130 → 83 переворота (залпы исчезли), смэш 5 → 3 crust-события
в правильном окне 64–75 с (волна с контактной ленты 55–75 с). Юнит-тесты:
автоэкспозиция 0, одиночное стойкое потемнение 1, сыр 0, тень лопатки 0.
Инструмент аудита: src/audit_video.py.

## Ручная разметка переворотов: доля ложных 85% (31.08.2026)

Инструмент: src/flip_gt.py — пул кандидатов из двух проходов (боевой +
чувствительный: flip_dl 11, confirm 2.5с, min_side 15с), 64 события, для
каждого лента кропов −8/−3/+1/+6/+12с вокруг котлеты (out/flip_review),
вердикты в data/flip_verdicts.json.

Размечено 58 из 64 (6 неоднозначных исключены).

| метрика | значение |
|---|---|
| настоящих переворотов найдено | 6 (t=472, 479, 555, 569, 583, 870) |
| ложных срабатываний | 33 |
| **точность (precision)** | **15%** |
| полнота (recall) по пулу | 100% |

Где живут ложные: сыр и снятие (>660с) — 27, жарка+сыр — 9, подготовка на
пустой плите — 8, укладка сырых — 8. То есть сигнал корки принимает за
переворот: укладку сыра (несмотря на b*-гейт — сыр ложится не мгновенно и
частично), проход и тень лопатки при снятии, а также тени на пустой плите
в фазе подготовки.

Вывод честный: на длинной реальной смене детектор переворота в текущем виде
непригоден (85% ложных), хотя на смэш-видео с чистыми переворотами он работает.
Полнота 100% означает, что сигнал видит все настоящие события — проблема
исключительно в отсечении чужих. Путь вперёд — классификатор лица котлеты
(ResNet18+логрег, 100% на held-out в оффлайн-прототипе) вместо порогов по
яркости: он различает «сырое/зажаренное/сыр» по текстуре, а не по светлоте.

## Классификатор лица котлеты: обучен, но потолок точности не в нём (31.08.2026)

**Модель.** 3456 кропов, размеченных через кластеризацию (ResNet18-эмбеддинги +
KMeans-10, вердикт по обзорному монтажу — одна человеческая оценка размечает
сотни кропов; смешанные кластеры выброшены). Признаки: замороженные
ImageNet-фичи + 12 цвето-текстурных статистик (сыр — гладкий жёлтый лист,
курица держит фактуру фарша). Сплит — чередование 60-секундных блоков: обычный
70/30 по времени отдал весь поздний сыр в валидацию и модель судили по виду,
которого она не видела (raw precision 0.51 → после блочного сплита 0.98).

| класс | precision | recall | n (val) |
|---|---|---|---|
| сырое | 0.979 | 0.942 | 537 |
| зажаренное | 0.934 | 0.964 | 559 |
| сыр | 0.932 | 0.883 | 325 |
| не котлета | 0.914 | 0.984 | 249 |
| **итого** | | **94.4%** | 1670 |

**Интеграция.** Переворот = стойкая смена лица (окно 3с до и после, согласие
85%), сыр — отдельное лицо (не переворот), класс «не котлета» блокирует
переворот у треков, съехавших на плиту/руку.

**Результат по метрике из ручной разметки: точность 15% → 21%, полнота 5/6.**
Плато: ни правило «сыр завершает котлету», ни 4-й класс, ни ужесточение окон
не сдвинули цифру. Диагностика причины (кропы вокруг ложных): 26 из 38 ложных
живут в фазе сыра и снятия, где трек **теряет свою котлету** — детекция
удерживается на пустой плите или на соседе, и любой сигнал на таком треке
бессмысленен. Классификатор при этом честен: класс «не котлета» составляет
лишь 2% детекций, то есть детектор действительно выдаёт «котлету» там, где её
уже нет.

**Вывод:** узкое место сместилось с сигнала переворота на удержание трека при
снятии котлеты. Классификатор лица — рабочий инструмент (94%), но его потолок
задан качеством того, что ему подают. Следующий шаг — не улучшать сигнал, а
чинить снятие: детектор должен терять котлету, когда её сняли, а не
перепрыгивать на соседнюю.

## Удержание трека при снятии: переворот как эпизод (31.08.2026)

Три гипотезы отпали на данных, прежде чем нашлась верная:
1. «Гейт расширяется для пропавших» — сузили, прыжков 129→131. Мимо.
2. «Треки съезжают на пустую плиту» — кропы показали обратное: трек ЧЕСТНО
   ведёт котлету, поднятую на лопатке. Метрика прыжков считала это ошибкой.
3. **Важная поправка к прежнему отчёту:** «0 телепортов» было артефактом —
   метрика мерила сглаженный центр, а EMA 0.7/0.3 гасит одиночный прыжок ниже
   порога 0.8r. По сырым детекциям прыжков было 129, а не 0.

Верное решение: переворот — это не «лицо изменилось», а **лицо изменилось
через эпизод манипуляции**. Тень, экспозиция и сыр меняют картинку без
эпизода; переворот имеет форму (подняли/сдвинули → устоялась другой стороной).
Плюс разделены роли фильтра: КФ-предсказание используется для ПОИСКА котлеты
(иначе при переносе сглаженный центр отстаёт и на одной котлете рождается три
трека), сглаживание — для отчётной позиции.

Итог по метрике ручной разметки: **точность 15% → 35%**, полнота 5/6,
сырых прыжков 129 → 108. Боевые константы: gate_max 1.8, lifted_after 2.0,
settle_time 4.0, flip_cooldown 45, use_kf="motion".

Остаток честно: 22 ложных из 34. Дальше упирается в качество кадра при
манипуляции (рука закрывает котлету целиком) — это уже вопрос ракурса камеры
на пилоте, а не алгоритма.

## Финальный аудит: оба сигнала переворота вместе (31.08.2026)

Отключение gap-сигнала при наличии лиц дало точность 45%, но полнота упала
2/6: после первого переворота ОБЕ стороны выглядят зажаренными, и
классификатор второй переворот не различает в принципе. Комбинация выигрывает
по F1 на ручной разметке: 0.49 (лицо+gap) против 0.45 (только лицо) и 0.38
(только эпизоды). Цифры выбрали архитектуру.

| | исходно | финал |
|---|---|---|
| переворотов на 16.8 мин | 130 (залпы 14–16) | 35 |
| точность | 15% | 34% |
| полнота | — | 5/6 |
| лишних колец | 4 разом | +0.29 в среднем |

Смэш: 4 переворота (2 по лицу, 2 по исчезновению), стороны 45–76с, залпов нет.
Ролик перевыпущен: ~/Downloads/grill-cook-smash-demo.mp4

## Разбор кадра заказчика: три причины лишних колец (31.08.2026)

1. **FLIP на котлете с сыром.** Лицо обновлялось только по завершении эпизода
   манипуляции, а сыр кладут на неподвижную котлету — флаг cheesed не ставился,
   отсчёт продолжался. Теперь лицо читается каждый кадр; котлета с сыром —
   бейдж DRESSED, из отсчёта выведена.
2. **Кольца на пустых местах.** Диагностика показала: трекер работает 1:1 с
   детектором (8 детекций — 8 колец), а лишние сидели на детекциях с
   уверенностью 0.18–0.32. birth_conf 0.30 → 0.55: лишних колец на смэше
   +0.13 → −0.49, на длинном +0.29 → +0.05.
3. **Кольца на лопатке/руке.** Детекции класса «не котлета» рождали треки.
   Запрет на рождение (но не на продление: precision класса 0.91, жёсткая
   фильтрация роняла полноту переворотов 5/6 → 4/6).

Цена: F1 переворотов 0.49 → 0.44 (флаг сыра выводит котлету раньше и глушит
часть поздних переворотов). Выбор сознательный: то, что видит повар, важнее
события в логе.

Следующий предел задаёт сам детектор — слабые срабатывания на жирных пятнах в
тёмной части кадра. Материал для дообучения собран: data/faces_other (535
негативов).

## Детектор v7: дообучение на негативах (31.08.2026)

Классификатор лица использован как чистильщик авторазметки: каждое предложение
детектора проверяется, всё названное «не котлета» (жир, лоток, лопатка, рука)
выбрасывается из меток и становится фоном. Плюс 22 кадра пустой плиты без
единого бокса. Датасет v3: 466 кадров / 5190 боксов train, 203 / 1698 val,
317 предложений отброшено. Дообучение 25 эпох от v6.

Прямой замер (доля предложений, которые классификатор считает не котлетой —
mAP тут слеп, так как метки порождены самим детектором):

| | v6 | v7 |
|---|---|---|
| предложений | 1011 | 988 |
| «не котлета» | 54 (5.3%) | **39 (3.9%)** |
| слабых (<0.5) | 58 (5.7%) | **30 (3.0%)** |

На спорном кадре t=104: фантомы 0.18 и 0.32 исчезли, слабейшая детекция 0.50,
класса «не котлета» нет. Порог рождения трека возвращён 0.55 → 0.40: маскировка
больше не нужна, а котлета под сыром читается на 0.50 и не должна теряться.

Важный урок про метрику: «лишние кольца» считались относительно числа
детекций, поэтому более чистый детектор выглядел в ней хуже. Для сравнения
детекторов она непригодна — нужен абсолютный счёт котлет в кадре.

## Покадровый аудит на v7 (31.08.2026)

| | первое видео (16.8 мин) | смэш (2.8 мин) |
|---|---|---|
| детекций/кадр | медиана 13, пик 19 | медиана 6, пик 9 |
| колец на экране | медиана 13, пик 18 | медиана 5, пик 9 |
| кадров с лишними кольцами | 18% | **10%** |
| переворотов | 38 (gap 33, face 5) | 4 (face 2, gap 2) |
| точность / полнота | 24% / 5 из 6 | залпов нет |

Проверка честности метрики: из 38 переворотов только 2 попали в моменты, не
размеченные вручную (t=577 — проход лопатки, t=657 — укладка сыра); оба
ложные, то есть 24% не занижены пропусками разметки.

Перекос v7: сигнал исчезновения даёт 33 из 38 переворотов. Более чистый
детектор чаще «теряет» котлету (перестал держать её жирным пятном), а gap-путь
срабатывает мгновенно, без требования устояться. Ужесточение порога паузы
(2.5/4/6с) поднимает точность до 31–50%, но роняет полноту до 1–2 из 6; F1
максимален на текущих настройках (0.37). Настоящее решение — не порог, а
камера: при верхнем ракурсе рука не закрывает котлету целиком и эпизодная
логика получает кадр «после».

## Демо-ролики на v7 (31.08.2026)

Ролик пересобран на боевом v7 + исправленной логике лица. Рендерер получил
`--start` (движок доигрывает всё до окна молча, кадры пишутся с нужной секунды)
и `--width`; чтение кадров стало последовательным — перемотка на каждый кадр в
1080p60 стоила дороже самого декодирования.

1. `~/Downloads/grill-cook-v7-smash.mp4` — смэш-смена целиком, 165 с,
   цели 42/35 с. То, на что заказчик прислал кадр, закрыто: котлета под сыром
   получает бейдж DRESSED и уходит из отсчёта, FLIP на ней больше не горит
   (пик — 7 бейджей одновременно). Переворотов 4: 66.8 с и 75.8 с (optimal,
   стороны 45 и 52 с), 151.4 с и 156.2 с (late). Колец 5 при 6 детекциях,
   кадров с лишними кольцами 10%.
2. `~/Downloads/grill-cook-v7-shift.mp4` — обычная смена, окно 470–560 с,
   90 с. Взято ради переворотов из ручной разметки: 481.6 и 491.2 (real),
   541.2 (real). Цели 270/150 с выставлены по самой сессии (медиана стороны
   146 с, p75 282 с, реальные первые стороны 260–304 с) — на 120/90 из
   прежнего коммита плита горит красным постоянно, и ролик врёт про повара.
   В окне видны все три реакции HUD: две зелёные (optimal), одна красная
   (late), одна оранжевая с переносом (early, carry +44 с).

Что в ролике честно видно как дефект: на хвосте смэша (после 145 с), когда
повар снимает котлеты, кольца ещё стоят на пустой плите. Разбор кадра t=152:
детекций 3, треков 6. Два кольца (#14, #22) — это 0.4-секундная витрина показа
(котлету только что подняли, кольцо гаснет через кадр, в движении незаметно).
Третье (#18) — настоящая ошибка детектора: сам жирно-сырный след на месте
снятой котлеты даёт бокс с уверенностью 0.72, и классификатор лица называет
его «сыр». Это материал для v8: негативы «след после снятия», собираются тем же
рецептом, что и v7 (классификатор чистит авторазметку + явные негативы).

## Слоты вместо треков: проверка гипотезы заказчика (31.08.2026)

Гипотеза: камера статична, котлета при жарке не движется, «скачет» только
детектор. Значит позиция должна фиксироваться после укладки (и смэша), а
переворот ловится отдельным сигналом и подтверждается цветом самой котлеты.

### Посылка верна, и это измерено

По ручной MOT-разметке (4 окна, 85 треков): котлета **стоит на месте 82–99%
жизни** (медиана 90%), делает **1 перемещение** за жизнь, и **90–100%
перемещений короче 0.5 r** — то есть это подвижка внутри того же места, а не
переезд.

По сырым дампам, без фильтра трекера: шаг детекции между кадрами p50 **0.004–
0.017 r**, p90 0.019–0.063 r. Но у 1.7–3.0% детекций шаг больше 0.25 r, у
0.7–1.2% — больше 0.5 r, максимум 1.5 r; ещё 1.5–2.7% детекций появляются
ниоткуда. При 5 fps и пятиминутной жизни котлеты это ~30 выбросов на котлету —
ровно то, что выглядит как «живая котлета».

Вывод: 97–98% времени детектор прибит к месту. Позицию незачем вести по кадрам,
её достаточно решить один раз.

### Прототип (src/slots.py, src/eval_slots.py)

Слот — это место, а не бокс. Детекции перестают быть объектами для сопоставления
и становятся ответом на локальный вопрос «моя котлета всё ещё здесь?». Рука над
слотом больше не стоит идентичности, потому что идентичность — это место.

| | продакшен | слоты |
|---|---|---|
| ход кольца (длинное) | 2.37 r/мин | **0.33 r/мин** |
| ход кольца (смэш) | 3.81 r/мин | **0.32 r/мин** |
| скачков >0.1 r | 3.9–7.0/мин | **0.5–0.7/мин** |
| id за смэш-сессию | 22 | **13** |
| кадров с лишними кольцами | 18% / 10% | 13% / 9% |

### Событие и вердикт по цвету (src/anchor_probe.py)

«Отдельный детектор переворотов» не нужно обучать: у закреплённого слота каждый
кадр те же пиксели, поэтому обычная разница кропа с самим собой даёт сигнал
манипуляции. Фон тихий (p50 0.02–0.045), события выбиваются в 10–60 раз
(до 4.9). Дальше цвет того же кропа до и после:

* **переворот** — L падает на 20+, b* стоит на месте;
* **сыр** — L растёт и b* сильно растёт вместе с ним;
* **снятие** — кроп возвращается к голой плите.

Проверка глазами на смэше: 8 из 8 подписей верны (4 переворота, 2 сыра,
2 снятия). На смэше найдена вся серия переворотов 56–71 с, которую продакшен
почти пропустил, а два его «переворота» на 151.4 и 156.2 оказались снятиями.

Проверка глазами на длинной смене, 10 вердиктов «переворот»: 4 настоящих
(550, 578, 865 и 487 — последний совпал с ручной разметкой), 4 ложных,
2 спорных. **Все четыре ложных — это снятия котлеты**, а не случайный шум.

### Честные ограничения

1. Пороги привязаны к съёмке: на смэше сыр даёт b* −40 (жёлтый на говядине), на
   курином филе того же контраста нет, и сыр уезжает в соседний класс. Нужна
   калибровка на SKU, а не одна константа.
2. Ошибки — это снятия, принятые за переворот. Лечится не порогом: у слота есть
   эталон голой плиты (свои же пиксели до укладки), и «после» надо сравнивать с
   ним, а не только смотреть на дельту.
3. Старая ручная разметка переворотов для новой схемы негодна: её кропы
   центрированы по треку старого движка и захватывают соседей — на t=434
   размечена бледная куриная котлета, которая не меняется, а переворачивают
   красную над ней. Позиционный матчинг это вскрыл: продакшен по месту, а не по
   времени, ловит 1 из 6, а 23 его флипа не попадают ни в один просмотренный
   момент. Прежние «5 из 6» и «24%» были завышены матчингом только по времени.
4. Класс «не котлета» как триггер события не годится: v7 специально учили не
   видеть руки, и на длинном видео он есть лишь в 5% кадров. Триггер должен
   идти от кропа, а не от детектора.

## Эталон голой плиты: что он умеет и чего не умеет (31.08.2026)

Задача была: отделить снятие от переворота, потому что все четыре ложных
вердикта на длинной смене оказались снятиями. Эталон реализован двумя способами
и оба измерены.

**Вариант 1 — снимок места до укладки** (кольцевой буфер кадров, медиана за
6 секунд перед рождением слота). Не работает: на смэше повар давит лопаткой
ровно на этом месте, а слот рождается тогда, когда котлету увидел ДЕТЕКТОР, —
на секунды позже, чем мясо там появилось. Отрисовка эталонов показала лопатку
и сырой фарш вместо плиты.

**Вариант 2 — кольцо вокруг слота в том же кадре** (аннулюс 1.15–1.9 r, из
которого вырезаны и соседние слоты, и все детекции). Эталон есть в 100% кадров,
но на вопрос «сняли ли котлету» он не отвечает: на 10 проверенных глазами
случаях центр-минус-кольцо по яркости, разбросу и зернистости не разделяет
пустое место и занятое. Причины физические: тёмная зажаренная котлета по
среднему Lab почти равна тёмной плите, а после снятия на месте остаётся жирный
след с мясной зернистостью.

**Что отвечает на этот вопрос — занятость слота.** На тех же 10 случаях: после
снятия слот занят 0% времени в следующие 1–8 секунд, после переворота — 50–100%.
Разделение полное. Снятие — вопрос присутствия, и закреплённый якорь отвечает
на него детектором, который в этом надёжен.

**Где кольцо действительно нужно — экспозиция.** Прежняя поправка считалась по
медиане яркости ДЕТЕКЦИЙ: когда повар переворачивает партию разом, все детекции
темнеют, и поправка вычитает ровно тот сигнал, который мы ищем (на s7 она дала
−43.8 и съела переворот). Кольцо этим не страдает — оно не меняется от того, что
котлету перевернули. Но как поправка оно шумит на ±25 пунктов и в одну сторону
убивает настоящий переворот, в другую рисует ложный. Поэтому кольцо работает
вето: если свет на этом месте уехал больше чем на 25 пунктов, вердикт не
выносится вообще (15 отказов на длинной смене, 1 на смэше).

### Что ещё пришлось починить, чтобы прогон стал честным

1. **Эпизод открывается по возмущению кропа, а не по исчезновению.** Смэш-котлету
   переворачивают на месте, детектор её часто не теряет — эпизодов было 10 на всю
   сессию и вся серия переворотов 57–71 с не существовала для движка. С кропом их
   40.
2. **Классификатор лица больше не накладывает вето на сыр.** На смэше он называет
   бледный сырой фарш сыром в 42% детекций (учили в другой кухне), и это молча
   съедало переворот. Теперь цвет решает первым: сыр двигает b* на 29–41 пункт,
   переворот — меньше чем на 6.
3. **Остывание и минимальная сторона стали долей от норматива.** Константа 45 с
   была настроена на стороны 150–270 с и запрещала любой переворот на смэше, где
   сторона живёт 35–42 с.
4. **Кольцо считается по кадру 640 px.** На полном 1080p это четверть миллиона
   пикселей на слот на кадр, прогон вставал намертво. Сейчас движок стоит
   4.5 мс/кадр на 9 слотах — с запасом для 25 fps на эдже.

### Прогон заново

| | продакшен | слоты |
|---|---|---|
| ход кольца, длинная / смэш | 2.37 / 3.81 r/мин | **0.33 / 0.32 r/мин** |
| скачков >0.1 r | 3.9 / 7.0 в мин | **0.5 / 0.7 в мин** |
| id за смэш-сессию | 22 | **13** |
| кадров с лишними кольцами | 18% / 10% | 13% / 9% |
| переворотов на длинной | 38 | **21** |
| переворотов на смэше | 4 (из них 2 — снятия) | **4, все настоящие** |

Смэш проверен глазами целиком: 4 вердикта из 4 — настоящие перевороты
(сырое розовое → зажаренное, лопатка в кадре). Продакшен на том же видео давал
4, из которых 151.4 и 156.2 были снятиями.

### Проверка глазами после перепрогона

Смэш: 4 вердикта из 4 настоящие (сырое розовое → зажаренное, лопатка в кадре).

Длинная смена, 12 вердиктов подряд: 5 настоящих (300, 461, 477, 480, 555 с —
бледная сторона до, золотистая корка после), 4 ложных, 3 спорных. Точность
порядка 50–60% против 7–24% у продакшена на той же разметке.

Главное: **снятий среди ложных больше нет ни одного** — та ошибка, ради которой
всё это делалось, закрыта занятостью слота. На её место вышла другая, узкая и
понятная: в момент вердикта место закрыто посторонним предметом — предплечьем
повара (449 с) или сосиской, которую катят по плите (539 и 542 с), — и цвет
этого сравнивается с котлетой.

Требование «место занято весь послеэпизодный интервал» (hold_frac 0.8) не
помогло против них: детектор считает и сосиску, и то, что под рукой, котлетой,
слот остаётся занят. Инвариант оставлен как разумный, но проблему решает не он,
а негативы этого рода в классификаторе лица — тот же рецепт, что дал v7.

## Классификатор v2: негативы рука / лопатка / сосиска (31.08.2026)

Добыча негативов оказалась бесплатной: движок слотов сам их находит. Место с
высоким возмущением кропа — это по определению место, над которым рука, перчатка
или лопатка, так что уже детектируемые эпизоды и есть майнер (`src/mine_negatives.py`).
Чужую еду пришлось искать отдельно: сосиску катят по плите 9 секунд, и именно на
ней сработали два ложных вердикта.

Кластеризация 1741 кандидата (K=30) дала чистые кластеры перчаток и предплечий
(3, 9) и смешанные с сыром кластеры лопаток (22, 24). Смешанные отфильтрованы по
жёлтизне (b* < 140), чтобы не научить модель звать сыр «не котлетой» — тот же
урок, что записан в метках прошлого раза: чистота важнее объёма. Негативов
535 → 1170 (перчатки 206, инструмент 114, сосиска 315).

| | было | стало |
|---|---|---|
| общая точность | 94.4% | 93.0% |
| руки и лопатки на отложенной половине | **48.7%** | **95.6%** |
| сыр (recall) | — | 0.825 |

Главное измерение — второе: старая модель звала «не котлетой» меньше половины
рук и лопаток, новая — 96%. Цена — сыр: 43 его кропа уехали в «другое», потому
что в кластерах лопаток сыр всё-таки присутствует. Для движка это дёшево: сыр
там теперь решается цветом (Δb), а «другое» означает всего лишь «не выносить
вердикт по этому кадру».

### Честно про сосиску

Блочный сплит режет её событие пополам (534–540 в трейн, 540–543 в вал), то есть
валидация видит почти те же кадры — её 93% не заработаны. Поставил чистый опыт:
обучил модель вообще без единого кропа сосиски и проверил на ней.

* модель без новых негативов вообще: «не котлета» на **11.4%** кропов сосиски;
* модель с руками и лопатками, но никогда не видевшая сосиску: **35.6%**;
* модель, видевшая сосиску: 93% (на почти-дубликатах).

То есть негативы-руки втрое поднимают шанс узнать незнакомый посторонний предмет,
но надёжным это не делает. Обобщения на новый предмет от 315 кропов одной сосиски
ждать нельзя — нужен разнообразный набор посторонних предметов, а не больше
кадров одного.

### Дообучение не сработало бы вообще: классификатор спрашивали не о той картинке

Первый перепрогон после дообучения дал 21 → 18 переворотов, и все три ложных
(рука на 449, сосиска на 539 и 542) остались. Диагноз оказался не про данные:
модель применялась к боксу детекции, а движок рассуждает о закреплённом кропе.
Когда сосиска лежит поперёк котлеты, бокс детекции читается как «сырое» с
уверенностью 0.63–0.82, а та же модель на закреплённом кропе даёт «не котлета»
пять кадров из пяти (и 200 из 200 на добытых кропах сосиски).

Перенёс чтение лица на якорь (`src/face_reader.py`, `face_model` в движке):
**18 → 10 переворотов, 8 из 10 настоящих по глазам** против 6 из 10 до этого.
Ушли все три ложных, плюс ещё пять. Остались два — оба про сыр и снятие
(724 и 747 с).

### Но вслепую это включать нельзя

На смэше то же изменение дало **ноль переворотов**: закреплённый кроп там
читается как «не котлета» в 85–100% кадров, и 27 вердиктов из 31 отклоняются.
Причина — сдвиг распределения: модель училась на кропах по боксу детекции, а
расплющенная смэш-котлета выходит за радиус якоря. Порогом не лечится: p(other)
у смэш-котлеты 0.96, у настоящей сосиски 0.95 — модель одинаково уверена.

Решение — самопроверка вместо доверия: место, которое занято и спокойно, держит
котлету по определению. Если модель на таком месте систематически говорит «не
котлета» (больше половины из 40+ спокойных наблюдений), слот перестаёт ей
верить и вето не применяется. На смэше это отключается само и 4 переворота
возвращаются, на длинной смене остаётся включённым.

### Итог по обоим видео

С самопроверкой длинная смена даёт те же 10 переворотов (самопроверка там не
срабатывает, лицо читается), смэш — свои 4. Ни одно видео не платит за другое.

| | продакшен | слоты, начало дня | слоты сейчас |
|---|---|---|---|
| переворотов на длинной | 38 | 21 | **10** |
| настоящих по глазам | ~7–24% по разметке | 5 из 9 судимых | **8 из 10** |
| переворотов на смэше | 4 (2 — снятия) | 4 настоящих | 4: 2 явных, 2 спорных |

Уточнение по смэшу: набор сменился на один элемент (70.0 → 64.4 через лицо), и
по лентам в нём 2 явно настоящих, 2 спорных (один начинается из-под лопатки, у
второго обе стороны уже подрумянены) и ни одного явно ложного.

Оставшиеся два ложных на длинной смене (724 и 747 с) — снова сыр и снятие
котлеты с сыром, то есть тот же класс, что закрывался занятостью, но в варианте
«сыр кладут / снимают вместе с сыром».

## Сыр закрыт: 7 из 7 (31.08.2026)

Оба оставшихся ложных вердикта (724 и 746 с) оказались одной причиной. Модель на
закреплённом кропе честно читает «сыр» за несколько секунд до каждого из них, но
флаг «котлета с сыром закончена» был переведён на цвет, а на бледном курином
филе сыр не двигает b* так, как на говядине (на смэше 29–41 пункт, здесь почти
ничего). Слот оставался кандидатом на переворот, и рука или лопатка над ним
давала нужный перепад яркости.

Флаг вернулся на лицо — но только там, где лицу доверяет самопроверка. На смэше
сыр по-прежнему решает цвет.

| длинная смена | было | стало |
|---|---|---|
| переворотов | 10 | **7** |
| настоящих по глазам | 8 из 10 | **7 из 7** |

Цена честная: вместе с двумя ложными ушёл один настоящий переворот (461 с) —
там переворачивали котлету, на которой уже лежал сыр, и флаг вывел её из
отсчёта раньше. Смэш не изменился: свои 4 вердикта на месте.
