# Пилот: веб-камера у плиты (US) → сервер (MacBook M5 Max) → HUD

## Топология

```
[веб-камера на штативе] --USB--> [ноутбук партнёра] --ffmpeg push RTSP/TCP--> 
  [Tailscale-туннель] --> [MacBook: mediamtx :8555 -> конвейер -> :8080] 
  --> [браузер на ноутбуке партнёра: /hud]
```

Партнёр **публикует** поток на наш сервер (push). Тянуть поток с его ноутбука
(pull) не выйдет без проброса портов на его роутере — push через NAT работает всегда.

## Наш пилот — конкретные адреса

`<ХОСТ>` = **100.85.247.67** (MagicDNS: `macbook-pro-galymzhan-2.tail35cfa2.ts.net`).

- Админка: http://100.85.247.67:8080/admin
- HUD: http://100.85.247.67:8080/hud
- Публикация камеры (macOS, готово к вставке):

```bash
ffmpeg -f avfoundation -framerate 30 -video_size 1280x720 -i "0:none" \
  -pix_fmt yuv420p -c:v libx264 -preset veryfast -tune zerolatency \
  -g 30 -b:v 3M -an -f rtsp -rtsp_transport tcp rtsp://100.85.247.67:8555/cam
```

## Шаг 0 — сеть (делается руками, один раз)

Оба ставят [Tailscale](https://tailscale.com/download) и входят в один tailnet
(достаточно одного бесплатного аккаунта: второй устройство добавляется по ссылке-приглашению).
После этого MacBook получает адрес вида `100.x.y.z` — он и есть `<ХОСТ>` ниже.

Проверка с ноутбука партнёра: `ping <ХОСТ>` должен отвечать (~150–250 мс US↔KZ — нормально).

## Шаг 1 — сервер (MacBook)

```bash
cd "Grill Cook"
.venv/bin/python app/server.py
```

Поднимает: relay mediamtx на `:8555` (TCP-only) и веб на `:8080`.
Админка: `http://<ХОСТ>:8080/admin`.

## Шаг 2 — публикация камеры (ноутбук партнёра)

macOS (ffmpeg: `brew install ffmpeg`; список камер: `ffmpeg -f avfoundation -list_devices true -i ""`):

```bash
ffmpeg -f avfoundation -framerate 30 -video_size 1280x720 -i "0:none" \
  -pix_fmt yuv420p -c:v libx264 -preset veryfast -tune zerolatency \
  -g 30 -b:v 3M -an -f rtsp -rtsp_transport tcp rtsp://<ХОСТ>:8555/cam
```

Windows (имя камеры: `ffmpeg -list_devices true -f dshow -i dummy`):

```bash
ffmpeg -f dshow -framerate 30 -video_size 1280x720 -i video="Integrated Camera" ^
  -pix_fmt yuv420p -c:v libx264 -preset veryfast -tune zerolatency ^
  -g 30 -b:v 3M -an -f rtsp -rtsp_transport tcp rtsp://<ХОСТ>:8555/cam
```

Камера: сверху-вбок на сковороду, штатив зафиксирован, вся жарочная
поверхность в кадре, без сильной контровой засветки. После запуска команда
молча «висит» — это нормально, поток идёт.

## Шаг 3 — админка

1. RTSP-ссылка: оставить `rtsp://127.0.0.1:8555/cam` (сервер читает со своего relay).
2. Параметры котлеты/плиты → «Рассчитать рекомендацию» → «применить» → «Сохранить цели».
   Домашняя плита тоньше по мощности индустриальной: не стесняйтесь править цели руками.
3. Завести смену (ФИО партнёра, сегодняшняя дата, часы).
4. «▶ Запустить». В статусе должны появиться fps камеры и обработки, ниже — превью с боксами.
5. Если дообученная модель мажет на домашней сковороде — переключить «Модель» на
   YOLO-World (запасная, zero-shot) и перезапустить конвейер.

## Шаг 4 — HUD (ноутбук партнёра)

`http://<ХОСТ>:8080/hud` — на весь экран, включить 🔊 звук (нужен один клик).

- кольцо-прогресс на каждой котлете, остаток времени и сторона A/B
- за 3 с — крупный обратный отсчёт с бипами, `FLIP` — пора
- переворот вовремя → 🔥 тост и стрик; рано → оранжевый; поздно → красная рамка
- при обрыве связи таймеры продолжают идти локально

## Заметки эксперимента

- Латентность US↔KZ добавит ~0.3–0.5 с к превью; сами таймеры от неё не зависят
  (дедлайны абсолютные, HUD ведёт отсчёт по своим часам с поправкой).
- Все события пишутся в `app/state/events.jsonl` — из них соберём разбор эксперимента.
- Безопасность: цифры системы — про консистентность. Готовность фарша проверяется
  термометром: 71°C / 160°F внутри.
